#include <stdio.h>
 
int main(void)
{
    int x=27; // 0b11011 соответсвенно надо получить 0b11101, то есть 29 в десятичной
    
// меняем биты местами
int swap_bits(int num)
{
    
    int bit1 = (x >> 1); // первый бит, второй считая справа налево
    int bit2 = (x >> 2); // второй бит, третий считая справа налево
    
//Проверка одинаковости битов
if (bit1 != bit2) 
{
    x ^= (1 << 1); //исключающее или для 1-го бита
    x ^= (1 << 2); //исключающее или для 2-го бита
}

return x;
}

printf("дано: %d\n", x);

int result = swap_bits(x);
printf("Ответ: %d\n", result);

return 0;
}
