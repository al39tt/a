#include <stdio.h>
 
int main(void)
{
    int x = 10; //определяем переменную
    int *gpt = &x; //jghtltkzt erfpftnkm yf gthtvyye. x
   
   printf("gpt = %p\n", gpt); //выводим адрес 
  
   printf("x = %d \n", x); //выводим значение
   
    return 0;
}
