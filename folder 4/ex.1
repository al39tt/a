#include <stdio.h>
 
int main()
{
    int x = 20; //Обмен без третьей переменной
    int y = 7;
    x += y; // (20+7=27)
    y *= x; // (27*7=189)
    x += y; // (189+27=216)
 
    printf("%d\n",x);
 
    return 0;
}
