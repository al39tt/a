#include <stdio.h>
  
int main(void)
{
   for(int i=11; i < 20; i++) // если результат деления 0, то переход к след. итерации
   {
       if(i % 2 == 0) continue; // определяем остаток от деления
    {
     printf("%d ", i);  
    }
   }
    return 0;
}
