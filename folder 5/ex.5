#include <stdio.h>
 
int main(void)
{
    
  int x = 17;
  int y = 54;
  int z = -3;
  
  if (x > y && x > z)
  {
        printf("max = x = %d\n", x);
    }
  else if (y > x && y > z)
   {
        printf("max = y = %d\n", y);
    }
  else
  {
        printf("max = z = %d\n", z);
    }
  return 0;
}
