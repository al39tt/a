#include <stdio.h>

int main() 
	{
   
    int arr[] = {71, 234, 4, 71, 7}; //массив
    int size = sizeof(arr) / sizeof(arr[0]);
    int *ptr = arr;//указаетль
    
    for (int i = 0; i < size; i++)
    {
        printf("%d", ptr[i]);
    }
  return 0;
} 
