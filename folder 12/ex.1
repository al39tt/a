#include <stdio.h>

int main() 
{
    char *arr[] = {"Tom", "Bob", "Sam"}; 
    int size = sizeof(arr) / sizeof(arr[0]);

    for (int i = 0; i < size; i++)
    {
        printf("%s\n", arr[i]);
    }
 
    return 0;
}
